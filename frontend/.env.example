# 프론트는 기본적으로 API 를 상대 경로로 호출하고 dev 서버가 프록시한다 (vite.config.js).
# 아래 값은 프론트를 정적 호스팅하고 API 가 다른 도메인에 있을 때만 설정한다.
# VITE_API_BASE_URL=https://api.example.com

# dev 서버 프록시 대상 (기본 http://127.0.0.1:8001)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8002
